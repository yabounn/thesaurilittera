{% extends 'base.html.twig' %}

{% block title %}Connexion !{% endblock %}

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('css/form/login.css')}}">
{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h2>Identifiez-vous</h2>
        <div class="jumbotron col-md-12 mt-4">
            <div class="row loginForm">
                <div class=" col-md-4 offset-md-1">
                    <!-- Form  -->
                    <form method="post">
                        {% if error %}
                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}
                        </div>
                        {% endif %}
                        <legend><i class="fa fa-lock" aria-hidden="true"></i> Connexion</legend>
                        <div class="form-group">
                            <label for="inputEmail" class="sr-only">Email</label>
                            <input type="email" value="{{ last_username }}" name="_username" id="inputEmail"
                                class="form-control" placeholder="Adresse email ..." required autofocus>
                        </div>
                        <div class="form-group">
                            <label for="inputPassword" class="sr-only">Password</label>
                            <input type="password" name="_password" id="inputPassword" class="form-control"
                                placeholder="Mot de passe ..." required>
                        </div>
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                        <div class="form-group">
                            <button type="submit" class="btn btn-success">Connexion !</button>
                        </div>
                        <input type="hidden" name="_target_path" value="{{ targetPath }}" />
                    </form>
                    <!-- /.form -->
                </div>

                <div class=" col-md-4 offset-md-2 signIn">
                    <h5>Créer un compte !</h5>
                    <!-- Button modal -->
                    <!-- <a class="btn btn-primary left-align modal-trigger" data-tooltip="voir"
                        data-target="{{ path('security_registration') }}" href="#modal1">Je m'inscris </a> -->
                    <!-- <button class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
                        <i class="fa fa-plus"></i> Je m'inscris
                    </button> -->
                    <a class="btn btn-primary" href="{{ path('security_registration') }}"><i class="fa fa-plus"></i> Je
                        m'inscris</a>
                </div>
            </div>
            <!-- /.loginForm -->
        </div>
        <!-- /.jumbotron -->

        <!-- Modal inscription -->
        <!-- <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalTitle"
            aria-hidden="true" data-target="{{ path('security_registration') }}">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="exampleModalTitle">Formulaire d'inscription</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        {{ render(controller('App\\Controller\\SecurityController::registration')) }}
                    </div>
                </div>
            </div>
        </div> -->
        <!-- /.modal inscription -->
    </div>
    <!-- /.row -->
</div>
{% endblock %}